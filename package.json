{
  "name": "zk-airdrop",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:circom": "cd circuits && circom airdrop.circom --r1cs --wasm --sym -l ./vendor -l ./vendor/circomlib -l ./vendor/circom-ecdsa",
    "setup:groth16": "cd circuits && npx snarkjs groth16 setup airdrop.r1cs ../powersOfTau28_hez_final_22.ptau airdrop_0000.zkey && printf \"codex\\n\" | npx snarkjs zkey contribute airdrop_0000.zkey airdrop_final.zkey && npx snarkjs zkey export verificationkey airdrop_final.zkey airdrop_verification_key.json"
  },
  "dependencies": {
    "snarkjs": "^0.7.3"
  }
}
